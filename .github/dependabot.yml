# Dependabot — GitHub Actions only.
# sir has zero external Go/Rust dependencies (by design).
# This config only monitors Action version pins for security updates.
version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    commit-message:
      prefix: "ci"
